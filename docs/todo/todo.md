列表顺序不代表优先级，仅为提出顺序。

1. [ ] 孔隙识别算法优化: 目前的 auto_threshold 函数仍然会将部分阴影误识别成孔隙。可以考虑计算区域内的灰度梯度来判断是否为孔隙（孔隙应当有边界处灰度梯度极高，内部灰度梯度较低的特征）
2. [ ] 只考虑了矩形全填充孔隙的计算，假如存在不规则固体，需要排除非样品区域的情况，则可能在处理流程中还需要引入 mask 图层概念。（是在整个线性流程中将传递的图像复杂化为 `dict[str, image]`，令 dict 不同的键保存不同含义的图像，例如 `{"image": ..., "mask": ...}`；还是将工作流编辑改成 Node Graph 形式？）
3. [ ] 识别粉末SEM图像的各颗粒中心点